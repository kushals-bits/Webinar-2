# Agent Guidelines for Webinar 2

## Notebook Execution
- Whenever executing Jupyter notebooks (`.ipynb`), always attach and preserve the execution outputs (standard output, stderr, charts, tables, return values, display data) within the notebook file after every execution.
- Save the notebook files in-place with the completed execution cells so the full execution record is visible.
